/**
 * Baseline Clinical Pain-Stop Rule threshold (0-10 scale).
 * If pain reaches or exceeds this threshold, the session stops immediately for safety.
 * This baseline is reconciled between the client engine and server database models.
 */
export const PAIN_STOP_THRESHOLD = 5;

